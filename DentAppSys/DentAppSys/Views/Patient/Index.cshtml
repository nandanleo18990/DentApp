@model IEnumerable<DentAppSys.Models.AppModel>


@{
    ViewBag.Title = "Index";
}
@if (Session["UserEmail"] != null)
{
    <div align="right" style="width:auto;background:#728ea7;">
        @*<strong>@Model.RegisterModel.Email</strong>*@
        @Html.ActionLink("Logout", "Logout", "User")
    </div>
}
else
{
    // @Html.ActionLink("Login&Register", "RegAndLogin", "User")

}
<section class="Patient-Info">

@*
     <div class="Info-title">
         Personal Informations
     </div>
    <div class="Info-Content">
         <div>
             <span>
                 Firstname :
             </span>
             @Model.RegisterModel.Firstname
         </div>
         <div>
             <span>
                 Lastname :
             </span>
             @Model.RegisterModel.Lastname
         </div>
         <div>
             <span>
                 Birthday :
             </span>
             @Model.RegisterModel.Birthday.ToString("MMMM dd,yyyy")
         </div>
         <div>
             <span>
                 Email :
             </span>
             @Model.RegisterModel.Email
         </div>
         <input type="submit" value="Edit Your Profile" class="" />
     </div>*@

    </section>

    <section class="Patient-Menu">

        <nav class="p-menu">
            <ul id="Menu" class="clearfix">
                <li>@Html.ActionLink("Appointment", "Make", "Appointment")</li>
                <li>@Html.ActionLink("History", "", "")</li>
                <li>@Html.ActionLink("Documents", "Index", "Documents")</li>
            </ul>

        </nav>

    </section>

    <section class="Patient-Dashboard">

        <div id="dashboard_left">
            <h1> Incoming Appointments</h1>
   @* @{
        var grid = new WebGrid(Model);

    }
    @grid.GetHTML();*@

        </div>

         <div id="dashboard_right">
        <br/>
        <h1> Recent Appointments</h1>


                           </div>
            </section>
